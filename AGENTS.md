# 项目代理说明

在进行任何修改之前，先阅读 [README.md](./README.md)，并将其作为本项目的首要规范。

如果任务要求与 README.md 冲突，默认以 README.md 为准，除非用户明确要求覆盖。

实现时请保持和仓库中描述的架构、启动流程、数据规则一致。

所有开发、修改、提交、推送操作默认都在 `dev` 分支下进行；除非用户明确要求，否则不要在 `main` 分支直接操作。

每次完成修改后，记得提交代码，并使用中文备注说明本次做了什么修改。

每次完成提交后，记得将当前 `dev` 分支推送到远端对应分支，确保远端与本地保持同步。
