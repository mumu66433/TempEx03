# 状态 - 策划设计代理

## 更新规则

- 本文件只记录策划设计代理的协作状态。
- 策划设计代理被要求清理上下文、重置对话或开启新对话前，必须先更新本文件。
- 新对话开始后，必须读取本文件，再结合 Git 实际状态恢复工作。

## 当前状态

- 当前日期：2026-04-25
- 角色：策划设计代理
- 代理名称：`Raman`
- 代理 ID：`019db98c-30d9-74b2-866f-11c5d5c77907`
- 工作树：`D:\Doc\codex\TempEx03`
- 分支：`dev`
- 远端分支：`origin/dev`
- 主要维护目录：`docs/策划方案`
- 日志前缀：`[策划]`
- 当前协作模式：单分支下按目录分工协作

## 最近状态

- 旧的 `design/planning` 分支记录仅作历史参考，不再作为当前恢复依据。
- 当前如需继续工作，应直接在 `dev` 分支下维护 `docs/策划方案`。
- 当前如需提交，只提交 `docs/策划方案` 与自身工作目录内改动，并及时推送到 `origin/dev`。

## 权限边界

- 可修改 `docs/策划方案`
- 其他目录默认只读
- 如需跨目录修改，必须先说明原因并征得用户同意

## 恢复检查命令

```powershell
git status --short --branch
git fetch origin --prune
git log --oneline --decorate -5
```

