# 状态 - PM / 审核管理代理

## 更新规则

- 本文件只记录 PM / 审核管理代理的协作状态。
- PM / 审核管理代理被要求清理上下文、重置对话或开启新对话前，必须先更新本文件。
- 如果 PM / 审核管理代理管理了子代理，必须先要求子代理更新各自状态文件，再更新本文件。
- 新对话开始后，必须读取本文件，再结合 Git 实际状态恢复工作。

## 当前状态

- 当前日期：2026-05-01
- 角色：PM / 审核管理代理
- 工作树：`D:\Doc\codex\TempEx03`
- 分支：`dev`
- 远端分支：`origin/dev`
- 当前协作模式：单仓库、单分支、按目录分工协作
- 写入本文件前的 `dev` 基线提交：`8400680 更新代理协作规则与日志前缀规范`
- 恢复上下文时如需确认最新状态，应以 `git fetch` 后的 Git 实际状态为准。
- 当前推进任务：`V0 前端 UI 真实数据落地`
- 当前 PM 产物：`docs/代理说明/PM_V0前端UI真实数据落地推进单.md`

## 已派生子代理

- 策划设计代理：`Raman`，ID `019db98c-30d9-74b2-866f-11c5d5c77907`
- UI 设计代理：`Curie`，ID `019db98c-31f4-7141-87c8-59eba1ed819b`
- 历史程序制作代理：`Boyle`，ID `019db98c-32d6-7633-902b-c53fde5596de`
- 自 2026-04-25 起，程序角色准备拆分为前端代理和后端代理，后续应以新角色分派任务。
- 子代理当前应共享 `D:\Doc\codex\TempEx03` 工作目录，并统一在 `dev` 分支下工作。

## 旧模式记录

- 下列独立工作树、独立角色分支、分支合并记录仅作为历史参考。
- 自 2026-04-25 起，恢复上下文和继续协作时，不再以旧的 `design/planning`、`design/ui`、`feature/program` 流程作为执行依据。

## 当前协作要求

- 所有代理统一在 `dev` 分支下工作。
- 所有代理只修改各自负责目录。
- 已完成且确认无误的改动需要及时提交并推送到 `origin/dev`。
- PM 创建子代理时，必须明确指定角色、维护目录、日志前缀和禁止触碰目录。

## 当前待办与风险

- 已按 PM 视角拆分 `V0 前端 UI 真实数据落地` 工作包，后续应按推进单分派 UI、后端、前端任务。
- 当前工作区存在 `server/prisma/dev.db` 未提交变更，PM 提交文档时不得误带入该数据库文件；是否提交应由对应后端/程序任务另行说明。
- 三选一、功法选择、功法升级仍需要后端契约和 PM 拍板，前端不应先硬编码玩法数据。
- 下次恢复上下文时，应优先按单分支目录协作规则重新确认各子代理角色。
- 如果子代理收到的任务描述没有明确角色和目录边界，必须先补充分派信息再开工。
- 如果本文件与 Git 实际状态不一致，以 Git 实际状态为准，并更新本文件。

## 恢复检查命令

```powershell
git status --short --branch
git branch -a -vv
git worktree list
git fetch origin --prune
git log --oneline --decorate -5
```
