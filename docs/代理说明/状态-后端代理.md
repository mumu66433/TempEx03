# 状态 - 后端代理

## 更新规则

- 本文件只记录后端代理的协作状态。
- 后端代理被要求清理上下文、重置对话或开启新对话前，必须先更新本文件。
- 新对话开始后，必须读取本文件，再结合 Git 实际状态恢复工作。

## 当前状态

- 当前日期：2026-04-25
- 角色：后端代理
- 工作树：`D:\Doc\codex\TempEx03`
- 分支：`dev`
- 远端分支：`origin/dev`
- 主要维护目录：`server`、`server/docs`
- 日志前缀：`[后端]`
- 当前协作模式：单分支下按目录分工协作
- 当前角色来源：由原“程序制作代理”拆分而来

## 最近状态

- 自 2026-04-25 起，后端实现任务应由后端代理承担。
- 当前如需继续工作，应直接在 `dev` 分支下维护 `server`、`server/docs`。
- 当前如需提交，只提交自身负责目录内改动，并及时推送到 `origin/dev`。

## 权限边界

- 可修改 `server`、`server/docs`
- `client`、`docs/策划方案`、`docs/原型制作` 默认只读
- 如需修改根目录工程脚本或工程配置，必须先说明原因并征得用户同意，或由 PM 明确指派

## 恢复检查命令

```powershell
git status --short --branch
git fetch origin --prune
git log --oneline --decorate -5
```
